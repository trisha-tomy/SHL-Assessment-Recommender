{
  "name": "shl-recommender-api",
  "build": {
    "command": "pip install -r requirements.txt"
  },
  "run": {
    "command": "gunicorn main:app --workers 1 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:7860"
  },
  "metadata": {
    "tags": ["flask", "api", "machine-learning"]
  }
}